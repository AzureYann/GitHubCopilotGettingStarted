# Hello World Console App

A simple Python console application that greets the user by name.

## Usage

Run the app with:

```bash
python3 hello_world.py
```

Enter your name when prompted, and the app will greet you!

## Requirements

- Python 3.6+

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
```